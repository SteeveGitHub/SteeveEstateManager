

/*
function Pictures({pictures}) {
    
    return (
        <div>
            

                {
                    pictures.map((picture) => {
                        
                        let image = require(picture.path)
                        return (
                            <div className="images" key={picture.name}>
                                <img src={image} alt="Maison 1" />
                            </div>
                        )
                    })
                }
            


        </div>
    )
}

export default Pictures;
*/
 